class Solution {
public:
    bool checkIfPangram(string s) {
        map<char,int>mp;
        int n = s.length();
        for(int i=0;i<n;i++){
            mp[s[i]]++;
        }
        return (mp.size() == 26)?true:false;

    }
};
